language: go

go:
  - 1.8.1
  - tip

before_script:
  - make

sudo: false

script: make test

notifications:
  email: false

branches:
  only:
    - master
    - development

matrix:
  allow_failures:
    - go: tip
